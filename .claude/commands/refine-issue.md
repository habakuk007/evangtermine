Read and follow the agent instructions in `.agents/agents/refine-issue.agent.md`.

Ignore the YAML frontmatter `tools:` array — use your own available tools instead.

Context for this task: $ARGUMENTS
